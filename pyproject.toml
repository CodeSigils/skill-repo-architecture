[project]
name = "skill-repo-architecture-maintenance"
version = "0.0.0"
description = "Maintainer tooling for the skill-repo-architecture repository"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
  "pyyaml==6.0.3",
  "ruff==0.15.21",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 120
target-version = "py313"
