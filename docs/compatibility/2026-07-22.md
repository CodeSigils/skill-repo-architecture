# Runtime Compatibility Report — 2026-07-22

Payload revision: the `repo-architecture-skill` identity migration derived from
`77afa4a`. These runs supersede evidence recorded under the former skill name.

This report records bounded local runs. It does not extrapolate to later client
versions, different models, or different installation mechanisms.

## Results

| Runtime | Version | Install path | Explicit | Implicit | Negative | State |
| --- | --- | --- | --- | --- | --- | --- |
| OpenAI Codex CLI | 0.133.0 | copied to `.agents/skills/repo-architecture-skill/` | pass | pass | pass | `workflow_verified` |
| Hermes Agent | 0.19.0 (`413ed6b9`) | copied under isolated `$HERMES_HOME/skills/`; workflow used configured external discovery | pass | pass | pass | `workflow_verified` |
| Claude Code | 2.1.159 | copied to `.claude/skills/repo-architecture-skill/` | inconclusive | not run | not run | `candidate` |
| Gemini CLI | not installed | documented `.agents/skills/` candidate | not run | not run | not run | `candidate` |

## Procedure and grading

The copied test payload was the complete canonical directory, including
`SKILL.md`, eight references, and `LICENSE.txt`. Positive prompts asked for a
Markdown-only repository classification, all four boundaries, and one
proportionate recommendation. The negative prompt asked for `17 + 25` and the
selected skill or `none`.

A positive run passed when it selected or demonstrably loaded
`repo-architecture-skill`, classified the repository as Markdown-only, mapped
all four boundaries, and avoided an unjustified generated payload. The negative
run passed only when it returned `42` and selected no skill.

Codex ran after the identity migration with `--ephemeral`,
`--ignore-user-config`, `--ignore-rules`, and a read-only sandbox. Explicit
selection, implicit discovery, and negative exclusion all passed. The implicit
run visibly loaded the renamed canonical `SKILL.md`.

Hermes installation was checked after the identity migration with an isolated
task-specific `HERMES_HOME`; `hermes skills list --source local` reported the
renamed copied skill as enabled. Workflow runs used the existing authenticated
profile and configured external discovery, with `--ignore-rules` and bounded
turns. All three grades passed. Hermes printed an asynchronous MCP shutdown
warning after successful answers; this is a client limitation, not a grading
failure.

Claude Code was invoked against the renamed project skill with project settings,
but the provider stopped with `Credit balance is too low`. Discovery and
behavior therefore remain inconclusive, and no compatibility claim is made from
the copied directory alone.

## Reproduction prompts

- Explicit: `$repo-architecture-skill Audit a repository whose only artifact
  is one Markdown SKILL.md. Return: selected skill; archetype; four boundaries;
  one recommendation.` Use the runtime's documented explicit invocation syntax.
- Implicit: `Design a repository for a portable Markdown-only agent skill.
  Return: selected skill; archetype; four boundaries; one recommendation.`
- Negative: `What is 17 plus 25? Return: answer; selected skill or none.`

Re-run this report after a material payload, fixture, prompt, grader, discovery
path, or runtime-version change.
