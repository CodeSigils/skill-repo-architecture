Audit this repository's skill architecture. Before inspecting the repository,
identify and read any installed skill relevant to the request. Do not modify
files and do not use network access.

Classify the repository archetype, map the authoring source, runtime payload,
install artifact, and maintainer infrastructure, then recommend only controls
supported by observed artifacts. Cite repository-relative evidence for the
classification and every recommendation.

Return the final result using the supplied JSON schema. Set `case_id` exactly to
`markdown-only-discovery-skill`. Preserve the canonical recommendation phrase
about separating external monitoring from pull-request validation.
